<title>Lista de Preguntas</title>
  <link rel="stylesheet" href="/public/css/paginaPrincipalStyle.css">
</head>

<body class="bg-dark">

  <main class="container">
    <div class="text-center mb-4">
      <h1 class="fw-bold">Lista de Preguntas</h1>
      <p class="text-muted">Acá podés ver, editar, aprobar o eliminar las preguntas.</p>
      <p class="text-muted">También podés filtrarlas según su tipo: reportadas, sugeridas o activas.</p>
    </div>

    <div class="list-group">
    {{#preguntas}}
    <div class="list-group-item bg-dark text-light border border-secondary mb-3 rounded">
        <h5 class="mb-2">
        <a 
            href="/gestionarPregunta?tipo={{tipo}}&id={{pregunta_id}}" 
            class="link-info fw-bold"
        > 
            {{pregunta}}
        </a>
        </h5>

        {{#cantidad_reportes}}
        <p class="mb-1 text-danger"><strong>Reportes:</strong> {{cantidad_reportes}}</p>
        {{/cantidad_reportes}}

        {{#usuario_sugirio}}
        <p class="mb-1 text-warning"><strong>Sugerida por:</strong> {{usuario_sugirio}}</p>
        {{/usuario_sugirio}}

        <ul class="list-group list-group-flush mt-3">
        {{#respuestas}}
        <li class="list-group-item bg-secondary text-light border border-dark d-flex justify-content-between align-items-center">
            <span>{{descripcion}}</span>
            {{#es_correcta}}
            <span class="badge bg-success">Correcta</span>
            {{/es_correcta}}
        </li>
        {{/respuestas}}
        </ul>
    </div>
    {{/preguntas}}

    {{^preguntas}}
    <div class="alert alert-warning text-center" role="alert">
        No hay preguntas.
    </div>
    {{/preguntas}}
    </div>

    <div class="text-center mt-5">
      <a href="/editor" class="btn btn-secondary">Volver</a>
    </div>
  </main>
</body>
</html>